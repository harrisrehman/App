{
  "name": "annex",
  "version": "0.5.72",
  "private": true,
  "type": "module",
  "scripts": {
    "version:write": "node scripts/write-version.mjs",
    "dev": "npm run version:write && vite --host",
    "build": "npm run version:write && tsc --noEmit && vite build && node scripts/inline-dist.mjs",
    "preview": "vite preview --host",
    "typecheck": "tsc --noEmit",
    "test": "tsc --noEmit && node scripts/test-engine.mjs && node scripts/test-version.mjs && node scripts/test-map.mjs && node scripts/test-ai.mjs",
    "cap:sync": "npm run build && node scripts/sync-android-version.mjs && npx cap sync android"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.2.1"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/app": "^8.1.1",
    "@capacitor/browser": "^8.0.4",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/splash-screen": "^8.0.2"
  }
}
